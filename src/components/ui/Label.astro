---
interface Props {
	className?: string;
}

const { className } = Astro.props;
---

<span class={`bg-sugar text-sugar-800 text-xs uppercase px-2.5 py-0.5 rounded border border-sugar-400 ${className}`}>
    <slot />
</span>
